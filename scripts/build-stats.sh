# @generated by xtask gen-scripts

#!/bin/bash

# Monitor build performance and cache statistics
# This script provides insights into build optimization effectiveness

set -euo pipefail

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Logging functions
log_info() {
    echo -e "${BLUE}[INFO]${NC} $1"
}

log_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1"
}

log_warning() {
    echo -e "${YELLOW}[WARNING]${NC} $1"
}

log_error() {
    echo -e "${RED}[ERROR]${NC} $1"
}

echo "📊 Build Performance Statistics"
echo "================================"

# 1. sccache statistics
if command -v sccache >/dev/null 2>&1; then
    echo "🔧 sccache Statistics:"
    sccache --show-stats
    echo
else
    echo "⚠️ sccache not installed - run ./scripts/optimize-build.sh to install"
    echo
fi

# 2. Cargo build timings
echo "⏱️ Recent Build Timings:"
if [[ -f "target/cargo-timings.html" ]]; then
    echo "✅ Detailed timings available at: target/cargo-timings.html"
    echo "   Run 'cargo build --timings' to generate timing data"
else
    echo "📝 No timing data found"
    echo "   Run 'cargo build --timings' to generate timing data"
fi
echo

# 3. Workspace analysis
echo "📦 Workspace Analysis:"
if command -v cargo >/dev/null 2>&1; then
    cargo tree --all-features --depth 1
else
    echo "⚠️ cargo not found"
fi
echo

# 4. Disk usage
echo "💾 Cache Usage:"
if [[ -d "${HOME}/.cache/sccache" ]]; then
    echo "sccache: $(du -sh "${HOME}/.cache/sccache" 2>/dev/null || echo "not accessible")"
else
    echo "sccache: not found"
fi

if [[ -d ".cargo/hakari" ]]; then
    echo "hakari: $(du -sh ".cargo/hakari" 2>/dev/null || echo "not accessible")"
else
    echo "hakari: not found"
fi

if [[ -d "target" ]]; then
    echo "target: $(du -sh "target" 2>/dev/null || echo "not accessible")"
else
    echo "target: not found"
fi
echo

# 5. Tool versions
echo "🛠️ Tool Versions:"
if command -v sccache >/dev/null 2>&1; then
    echo "sccache: $(sccache --version 2>/dev/null | head -1 || echo "version unknown")"
fi

if command -v cargo-hakari >/dev/null 2>&1; then
    echo "cargo-hakari: $(cargo-hakari --version 2>/dev/null || echo "version unknown")"
fi

if command -v cargo-nextest >/dev/null 2>&1; then
    echo "cargo-nextest: $(cargo-nextest --version 2>/dev/null || echo "version unknown")"
fi

if command -v cargo >/dev/null 2>&1; then
    echo "cargo: $(cargo --version 2>/dev/null || echo "version unknown")"
fi

if command -v rustc >/dev/null 2>&1; then
    echo "rustc: $(rustc --version 2>/dev/null || echo "version unknown")"
fi
echo

# 6. Performance recommendations
echo "💡 Performance Recommendations:"
echo "================================"

# Check if optimizations are enabled
if [[ "${RUSTC_WRAPPER:-}" == "sccache" ]]; then
    echo "✅ sccache is enabled"
else
    echo "⚠️ sccache not enabled - set RUSTC_WRAPPER=sccache"
fi

if [[ -f ".cargo/config.toml" ]]; then
    echo "✅ .cargo/config.toml found"
else
    echo "⚠️ .cargo/config.toml missing - run ./scripts/optimize-build.sh"
fi

if [[ -f ".cargo/aliases.toml" ]]; then
    echo "✅ .cargo/aliases.toml found"
else
    echo "⚠️ .cargo/aliases.toml missing - run ./scripts/optimize-build.sh"
fi

if command -v cargo-hakari >/dev/null 2>&1; then
    echo "✅ cargo-hakari available"
else
    echo "⚠️ cargo-hakari not available - run ./scripts/optimize-build.sh"
fi

if command -v cargo-nextest >/dev/null 2>&1; then
    echo "✅ cargo-nextest available"
else
    echo "⚠️ cargo-nextest not available - run ./scripts/optimize-build.sh"
fi

echo
echo "🚀 For optimal performance:"
echo "  • Run './scripts/optimize-build.sh' to install all tools"
echo "  • Use 'cargo dev-fast' for fast development builds"
echo "  • Use 'cargo test-parallel' for parallel testing"
echo "  • Monitor cache hit rates with this script"
echo "  • Consider using nightly toolchain for parallel compilation" 
