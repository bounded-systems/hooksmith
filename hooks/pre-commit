#!/bin/bash
set -e

echo "🔍 Running pre-commit validation..."

# Run Rust-based pre-commit hook
if ! cargo run -p git-hooks -- pre-commit; then
    echo "❌ Pre-commit validation failed"
    exit 1
fi

echo "✅ Pre-commit validation passed"
