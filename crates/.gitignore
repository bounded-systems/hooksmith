# Rust crate-specific artifacts (scoped to crates directory only)
/target/
Cargo.lock
*.rlib
*.rmeta
*.so
*.dylib
*.dll
*.exe

# IDE files (scoped to crates directory only)
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Build artifacts (scoped to crates directory only)
*.o
*.a
*.d

# Generated files (scoped to crates directory only)
bindings.rs
*.wit

# Test artifacts (scoped to crates directory only)
test-*.rs
*_test.rs
