# Test-specific artifacts (scoped to tests directory only)
/target/
*.exe
*.bin
*.out

# Test output files (scoped to tests directory only)
*.test-output
*.test-result
*.test-log

# Temporary files (scoped to tests directory only)
*.tmp
*.temp
*.bak
*.log

# OS files
.DS_Store
Thumbs.db

# IDE files (scoped to tests directory only)
.vscode/
.idea/
*.swp
*.swo

# Test data files (scoped to tests directory only)
test-data/
fixtures/
*.test-data
*.fixture

# Coverage reports (scoped to tests directory only)
coverage/
*.lcov
*.gcov
*.gcda
*.gcno

# Test databases (scoped to tests directory only)
*.db
*.sqlite
*.test.db
