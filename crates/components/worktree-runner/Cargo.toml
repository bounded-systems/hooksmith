# @generated by xtask gen-config
# @checksum: b4944d22

# @generated
# This file is automatically generated. Do not edit manually.
# Generated by: Hooksmith xtask

# This file is auto-generated by `cargo eval bootstrap.rs`
# Generated file: components/worktree-runner/Cargo.toml
# Do not edit this file manually - changes will be overwritten

[package]
name = "worktree-runner"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[package.metadata.component]

[features]
default = []
# Enable for host-native builds and testing
host = ["wit-bindgen"]

[dependencies]
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
serde_yaml = "0.9"
tracing.workspace = true
tracing-subscriber = "0.3"
thiserror.workspace = true
chrono = { version = "0.4", features = ["serde"] }
tokio = { version = "1.0", features = ["fs", "macros", "rt", "rt-multi-thread", "process"] }
tempfile = "3.8"
clap = { version = "4.0", features = ["derive"] }

# Kubernetes CRD support
kube = { version = "4.2", features = ["runtime", "derive"] }
kube-derive = "4.2"
kube-runtime = "4.2"
# MUST track the k8s-openapi that `kube` pulls (#135). Cargo treats 0.27 and
# 0.28 as incompatible, so a stale pin here does not lose a race -- it puts BOTH
# in the tree, and the copy kube pulled has no v1_* feature enabled, so
# k8s-openapi's build script panics: "None of the v1_* features are enabled".
#
# THE SUPPORTED KUBERNETES VERSION MOVES 1.31 -> 1.32, AND THAT IS NOT INCIDENTAL.
# k8s-openapi 0.28 DROPPED the v1_31 feature; v1_32 is the lowest it offers. So
# taking kube 4.2 is not a version bump that happens to touch a feature name --
# it RAISES THE MINIMUM API SERVER this binary supports. A cluster on 1.31 is no
# longer a target.
#
# v1_32 is chosen as the LOWEST feature 0.28 still offers, so the floor rises by
# exactly one minor and no further.
k8s-openapi = { version = "0.28", features = ["v1_32"] }
schemars = { version = "1.2", features = ["derive"] }
futures = "0.3"
futures-util = "0.3"

# Remove which dependency for WASM compatibility
# which.workspace = true
# wit-bindgen is optional and only used for host builds
wit-bindgen = { version = "0.52", optional = true }

[[bin]]
name = "crd-cli"
path = "src/bin/crd-cli.rs"
