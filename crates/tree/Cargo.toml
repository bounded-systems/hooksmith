[package]
name = "dircheck-tree"
version = "0.1.0"
edition = "2021"
authors = ["Pushd Team"]
description = "Validate directory structure using git ls-tree"
license = "MIT"

[lib]
name = "dircheck_tree"
path = "src/lib.rs"

[dependencies]
hooksmith-core = { path = "../core" }
