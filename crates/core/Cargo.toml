[package]
name = "hooksmith-core"
version = "0.1.0"
edition = "2021"
authors = ["Pushd Team"]
description = "Core validation logic for Hooksmith Git pipeline"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
git2 = "0.20"
sha2 = "0.10"
