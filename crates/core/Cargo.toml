[package]
name = "dircheck-core"
version = "0.1.0"
edition = "2021"
authors = ["Pushd Team"]
description = "Core validation logic for directory and file structure checking"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
