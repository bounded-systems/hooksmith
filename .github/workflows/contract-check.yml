# @generated by xtask gen-config

# @generated
# This file is automatically generated. Do not edit manually.
# Generated by: Hooksmith xtask

# GENERATED FILE - DO NOT EDIT
# This file is automatically generated by xtask
# To modify this file, update the source and regenerate

name: Contract Check System

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  contract-check:
    name: Contract Check & Validation
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Full history for trend analysis

    - name: Setup Rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true
        components: rustfmt, clippy

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-

    - name: Install dependencies
      run: |
        cargo install cargo-audit
        cargo install cargo-tarpaulin --version 0.20.1

    - name: Build project
      run: cargo build --workspace

    - name: Run contract check (strict mode)
      run: cargo run -p xtask -- contract-check --strict --trend --verbose
      continue-on-error: true

    - name: Generate status report
      run: |
        echo "## 📊 Contract Check Status Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Migration Progress" >> $GITHUB_STEP_SUMMARY
        cargo run -p xtask -- status migration-progress --format markdown >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### File Type Analysis" >> $GITHUB_STEP_SUMMARY
        cargo run -p xtask -- status file-types --format markdown >> $GITHUB_STEP_SUMMARY

    - name: Upload trend data
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: contract-check-trends
        path: status-trends/
        retention-days: 30

    - name: Comment PR status
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          const fs = require('fs');
          const summary = fs.readFileSync(process.env.GITHUB_STEP_SUMMARY, 'utf8');
          
          const comment = `## 🔗 Contract Check Results
          
          ${summary}
          
          ---
          *Generated by Hooksmith Contract Check System*
          `;
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: comment
          });

  validation:
    name: Comprehensive Validation
    runs-on: ubuntu-latest
    needs: contract-check
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    - name: Run tests
      run: cargo test --workspace

    - name: Run clippy
      run: cargo clippy --workspace -- -D warnings

    - name: Run fmt check
      run: cargo fmt --all -- --check

    - name: Security audit
      run: cargo audit

    - name: Validate generated files
      run: cargo run -p xtask -- validate-generated --strict

    - name: Validate headers
      run: cargo run -p xtask -- validate-headers --strict

  quality-gates:
    name: Quality Gates
    runs-on: ubuntu-latest
    needs: [contract-check, validation]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    - name: Code coverage
      run: |
        cargo tarpaulin --out Xml --output-dir coverage
        cargo tarpaulin --out Html --output-dir coverage

    - name: Upload coverage reports
      uses: actions/upload-artifact@v3
      with:
        name: coverage-reports
        path: coverage/
        retention-days: 30

    - name: Check migration progress threshold
      run: |
        # Extract migration progress percentage
        PROGRESS=$(cargo run -p xtask -- status migration-progress --format json | jq -r '.progress_percentage')
        
        # Check if progress is above threshold (e.g., 5%)
        if (( $(echo "$PROGRESS < 5" | bc -l) )); then
          echo "❌ Migration progress ($PROGRESS%) is below threshold (5%)"
          exit 1
        else
          echo "✅ Migration progress ($PROGRESS%) meets threshold"
        fi

    - name: Check file type count
      run: |
        # Extract file type count
        TYPE_COUNT=$(cargo run -p xtask -- status file-types --format json | jq '.file_types | length')
        
        # Check if file types are below threshold (e.g., 15)
        if [ "$TYPE_COUNT" -gt 15 ]; then
          echo "❌ File type count ($TYPE_COUNT) is above threshold (15)"
          exit 1
        else
          echo "✅ File type count ($TYPE_COUNT) meets threshold"
        fi

  documentation:
    name: Documentation Generation
    runs-on: ubuntu-latest
    needs: [contract-check, validation]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    - name: Generate documentation
      run: |
        cargo run -p xtask -- gen-docs-comprehensive --all
        cargo run -p xtask -- gen-readme
        cargo run -p xtask -- gen-schema-docs --pdf --html --epub

    - name: Upload documentation
      uses: actions/upload-artifact@v3
      with:
        name: documentation
        path: docs/
        retention-days: 30

    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./docs
        force_orphan: true

  summary:
    name: Summary Report
    runs-on: ubuntu-latest
    needs: [contract-check, validation, quality-gates, documentation]
    if: always()
    
    steps:
    - name: Generate summary
      run: |
        echo "## 🎯 Contract Check Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Job Results:" >> $GITHUB_STEP_SUMMARY
        echo "- Contract Check: ${{ needs.contract-check.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- Validation: ${{ needs.validation.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- Quality Gates: ${{ needs.quality-gates.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- Documentation: ${{ needs.documentation.result }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [[ "${{ needs.contract-check.result }}" == "success" && "${{ needs.validation.result }}" == "success" && "${{ needs.quality-gates.result }}" == "success" ]]; then
          echo "✅ All checks passed! Project is ready for merge." >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ Some checks failed. Please review the issues above." >> $GITHUB_STEP_SUMMARY
        fi
