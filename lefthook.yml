# @generated by xtask gen-files --file-type=yml
# @checksum: e3b0c442

# Enhanced Lefthook configuration with Hooksmith checksum validation
# This file is automatically generated. Do not edit manually.

pre-commit:
  parallel: true
  commands:
    # Code formatting and linting
    fmt:
      glob: "*.rs"
      run: cargo fmt --all -- --check
    
    clippy:
      glob: "*.rs"
      run: cargo clippy --all-targets --all-features -- -D warnings
    
    # File extension validation
    validate-extensions:
      run: cargo run --bin validate-file-extensions {staged_files}
    
    # Checksum validation for generated files
    validate-checksums:
      run: cargo run --bin validate-checksums {staged_files}
    
    # Contract validation
    contract-check:
      run: cargo run -p xtask -- contract-check --staged-only --strict
    
    # Tests
    test:
      run: cargo test --all-targets --all-features

pre-push:
  parallel: true
  commands:
    # Security audit
    audit:
      run: cargo audit
    
    # Comprehensive checksum validation
    validate-all-checksums:
      run: cargo run -p xtask -- validate-checksums --strict
    
    # Generated files check
    check-generated:
      run: cargo xtask check --strict
    
    # Contract validation
    contract-check:
      run: cargo xtask contract check --strict

commit-msg:
  commands:
    conventional:
      run: cargo run -p xtask -- validate-commit-msg --file "$1"


