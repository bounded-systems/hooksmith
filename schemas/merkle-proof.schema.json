# GENERATED FILE - DO NOT EDIT
# This file is automatically generated by xtask
# To modify this file, update the source and regenerate

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Merkle Proof",
  "description": "Schema for Merkle tree proofs in contract validation",
  "type": "object",
  "properties": {
    "merkle_proof": {
      "type": "object",
      "properties": {
        "root_hash": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$",
          "description": "Root hash of the Merkle tree"
        },
        "path": {
          "type": "array",
          "description": "Path from leaf to root",
          "items": {
            "type": "object",
            "properties": {
              "direction": {
                "type": "string",
                "enum": ["left", "right"],
                "description": "Direction of the sibling node"
              },
              "hash": {
                "type": "string",
                "pattern": "^sha256:[a-f0-9]{64}$",
                "description": "Hash of the sibling node"
              }
            },
            "required": ["direction", "hash"],
            "additionalProperties": false
          },
          "minItems": 0
        },
        "leaf_hash": {
          "type": "string",
          "pattern": "^sha256:[a-f0-9]{64}$",
          "description": "Hash of the leaf node"
        },
        "leaf_index": {
          "type": "integer",
          "minimum": 0,
          "description": "Index of the leaf in the tree"
        },
        "tree_depth": {
          "type": "integer",
          "minimum": 0,
          "description": "Depth of the Merkle tree"
        },
        "total_leaves": {
          "type": "integer",
          "minimum": 1,
          "description": "Total number of leaves in the tree"
        }
      },
      "required": ["root_hash", "path", "leaf_hash", "leaf_index"],
      "additionalProperties": false
    },
    "scope": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["char", "line", "chunk", "file", "directory", "repository"],
          "description": "Type of scope this proof covers"
        },
        "identifier": {
          "type": "string",
          "description": "Unique identifier for this scope"
        },
        "parent_scope": {
          "type": "string",
          "description": "Parent scope identifier"
        },
        "children": {
          "type": "array",
          "description": "Child scope identifiers",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["type", "identifier"],
      "additionalProperties": false
    },
    "validation": {
      "type": "object",
      "properties": {
        "validated_at": {
          "type": "string",
          "format": "date-time",
          "description": "When this proof was validated"
        },
        "validated_by": {
          "type": "string",
          "description": "Tool that validated this proof"
        },
        "contract_type": {
          "type": "string",
          "description": "Type of contract being validated"
        },
        "validation_result": {
          "type": "string",
          "enum": ["valid", "invalid", "pending"],
          "description": "Result of validation"
        },
        "errors": {
          "type": "array",
          "description": "Validation errors found",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["validated_at", "validated_by", "contract_type", "validation_result"],
      "additionalProperties": false
    }
  },
  "required": ["merkle_proof", "scope", "validation"],
  "additionalProperties": false
} 
